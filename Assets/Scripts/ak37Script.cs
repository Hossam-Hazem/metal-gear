using UnityEngine;
using System.Collections;

public class ak37Script : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
        this.transform.Rotate(0, 3, 0);
	}
}
